[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  
# Esto asegura que si alguien refresca en una ruta que no existe, vea tu página 404 personalizada
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404